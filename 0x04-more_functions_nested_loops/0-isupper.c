#include "main.h"

/**
 * int _isupper(int c) - Returns 1 if c is uppercase and Returns 0 otherwise
 * @c: input integer
 * Return: int.
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		Return (1);
	else
		Return (0);
}
